---
name: deep-interview
description: Sunset stub — use $plan --interview instead
---

# Deep-interview — removed

`$deep-interview` was removed in OMX 0.21; use `$plan --interview` instead.

The interview workflow is now an optional mode of the single planning skill `$plan`. Use `$plan --interview` (or a broad prompt) to trigger Socratic clarification before planning.

Migration: replace `$deep-interview` with `$plan --interview`.

Task: {{ARGUMENTS}}
