---
name: ralplan
description: Sunset stub — use $plan instead
---

# Ralplan — removed

`$ralplan` was removed in OMX 0.21; use `$plan` instead.

`$ralplan` was an alias of `plan --consensus` and the consensus mode itself has been removed. All planning now goes through the single `$plan` skill (`$plan --interview` for clarification, `$plan --review` for evaluation).

Migration: replace `$ralplan` with `$plan` in your prompt.

Task: {{ARGUMENTS}}
